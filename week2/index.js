// function sumOfSomething(a, b, fn) {
//   return fn(a) + fn(b);
// }

// console.log(sumOfSomething(2, 3, (val) => val * val));

// async function -> gets pushed in the queue
// readFile
// fetch("https://www.google.com");

// Promises -> for async functions to make them sync

// generally we wrap around a async function

// async function func() {
//   const ans = await promisifyFunction(1000);
//   ans.then(() => {
//     console.log("yayay");
//   });
// }

// func();

